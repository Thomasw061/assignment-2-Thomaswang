
Two things done well:

sharedDenom is extracted into a named variable, making the repeated term readable and the divide by zero risk obvious.
Single responsibility functions make main read as a clean, step-by-step sequence.

Two potential problems:

No zero division guard if sharedDenom is zero, the program silently returns nan.
The code is also vbery complex to my simple one where there is a lot less lines

Would I trust it? Yes The math and structure are solid, and ai is better than me at coding, but the missing zero-division guard is a real reliability issue. I'd fix that before using it in production.
~
~
